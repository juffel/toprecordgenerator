<div id="cover_container">
    <%= image_tag(@record.image_url)%>
    <div id="interpret"><%= @record.interpret %></div>
    <div id="album"><%= @record.album %></div>
    <div id="single"><%= @record.single %></div>
</div>
<hr>
<%= link_to "neu generieren", controller: "home", action: "new" %>
<table>
    <tbody>
        <tr>
            <td><b>Interpret:</b></td>
            <td><%= @record.interpret %></td>
        </tr>
        <tr>
            <td><b>Album:</b></td>
            <td><%= @record.album %></td>
        </tr>
        <tr>
            <td><b>Single:</b></td>
            <td><%= @record.single %></td>
        </tr>
        <tr>
            <td><b>Cover:</b></td>
            <td><%= image_tag(@record.image_url) %></td>
        </tr>
    </tbody>
</table>

